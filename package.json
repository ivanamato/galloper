{
  "name": "galloper",
  "private": true,
  "version": "0.2.0",
  "license": "MIT",
  "author": "Ivan Amato",
  "type": "module",
  "bin": {
    "galloper": "dist/run-llm-session.js"
  },
  "scripts": {
    "build": "tsc",
    "prerun": "npm run build",
    "run": "node dist/run-llm-session.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:all": "npm run build && vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "ignore": "^5.2.4",
    "picomatch": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/picomatch": "^4.0.3",
    "@vitest/coverage-v8": "^1.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
